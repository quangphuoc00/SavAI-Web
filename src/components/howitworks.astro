---
const steps = [
  {
    title: "Step 1",
    description: "Add your Costco receipts to the app to track your recent purchases."
  },
  {
    title: "Step 2",
    description: "We'll monitor your purchases for price drops and send you price adjustment alerts."
  },
  {
    title: "Step 3", 
    description: "Collect your price adjustment refund from Costco & enjoy your savings!"
  }
];
---

<div class="py-8 sm:py-12">
  <div class="mx-auto max-w-7xl px-6 lg:px-8">
    <div class="mx-auto max-w-2xl text-center">
      <h2 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">How It Works</h2>
    </div>
    <div class="mx-auto mt-2 max-w-2xl lg:max-w-none">
      <dl class="grid max-w-xl grid-cols-1 gap-x-8 gap-y-16 lg:max-w-none lg:grid-cols-3">
        {
          steps.map((step) => (
            <div class="flex flex-col items-start">
              <dt class="mt-4 font-semibold text-gray-900">{step.title}</dt>
              <dd class="mt-2 leading-7 text-gray-600">{step.description}</dd>
            </div>
          ))
        }
      </dl>
    </div>
  </div>
</div> 